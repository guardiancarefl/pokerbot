# Phase 2d GPU smoke test: 5 iters at the validation config size.
# Purpose: confirm GPU code path works, no hangs, no NaNs, checkpoints save.
# Should complete in ~1 minute on RTX 4090.
tag: phase2d_gpu_smoke
abstraction_path: runs/abstraction_20260521_223018/abstraction.pkl
# Structured game params (preferred over legacy game_str).
# starting_stack below is shared between the game (per-player chip count)
# and the solver (TrainConfig.starting_stack).
num_players: 2
big_blind: 100
small_blind: 50
hidden_dim: [512, 512]
n_iterations: 5
traversals_per_iter: 50
train_steps_per_iter: 100
batch_size: 32
learning_rate: 0.001
buffer_capacity: 100000
starting_stack: 20000
bucket_runouts: 30
max_traversal_depth: 600
seed: 2026
checkpoint_every: 5
