# Smoke config for Phase 2b: tiny stacks, tiny networks, short run.
# Goal: confirm pipeline runs end-to-end and produces sensible loss trajectories.
tag: smoke
abstraction_path: runs/abstraction_20260521_223018/abstraction.pkl
# Structured game params (preferred over legacy game_str).
# starting_stack below is shared between the game (per-player chip count)
# and the solver (TrainConfig.starting_stack).
num_players: 2
big_blind: 100
small_blind: 50
hidden_dim: [64, 64]
n_iterations: 20
traversals_per_iter: 50
train_steps_per_iter: 100
batch_size: 32
learning_rate: 0.001
buffer_capacity: 20000
starting_stack: 2000
bucket_runouts: 30
max_traversal_depth: 200
seed: 2026
checkpoint_every: 5
