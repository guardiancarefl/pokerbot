# Phase 2b target config: still tiny but larger sample budget.
# Intended for a CPU run (Contabo) before moving to Phase 2d on RunPod 4090.
tag: phase2b
abstraction_path: runs/abstraction_20260521_223018/abstraction.pkl
# Structured game params (preferred over legacy game_str).
# starting_stack below is shared between the game (per-player chip count)
# and the solver (TrainConfig.starting_stack).
num_players: 2
big_blind: 100
small_blind: 50
hidden_dim: [64, 64]
n_iterations: 100
traversals_per_iter: 100
train_steps_per_iter: 200
batch_size: 64
learning_rate: 0.001
buffer_capacity: 100000
starting_stack: 2000
bucket_runouts: 40
max_traversal_depth: 200
seed: 2026
checkpoint_every: 10
