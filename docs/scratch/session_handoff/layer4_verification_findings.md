# Layer 4 Part A — Verification Findings

FINDING A.1: archetype6.ArchetypePolicy.select_action(parsed, state, rng, mode='sample') -> int (sampled chip-action). Distribution is computed internally (archetype6.py:173-184) via archetypes.archetype_policy(...) which DOES return the full 7-slot ndarray distribution as its public return type — so the distribution surface is already exposed by archetypes.py directly; no refactor needed to evaluate likelihoods.

FINDING A.2: NAMED_ARCHETYPES is 5 EXPLICIT in-code dataclass instances (NIT/TAG/LAG/STATION/MANIAC with hand-set play_quantile_by_street + aggression scalars); the per-street equity quantile thresholds are LOADED from runs/archetype_design/bucket_equity_analysis_6max.json. archetype_policy is deterministic in its body (rng is taken but never used — grep confirms rng.X appears only in OpponentPool.sample_opponent at archetypes.py:325-327, not in archetype_policy itself). Likelihood evaluation P(observed_action | archetype, state) is one archetype_policy call per archetype = 5 deterministic O(1) calls per action observation — same order as the existing observer.update cost; LIKELIHOOD-EVALUABLE YES (one caveat: requires the opponent's bucket_id, which we don't observe — solvable by either (a) marginalizing over a uniform-population bucket prior, ~200 extra calls per archetype, still microseconds, or (b) conditioning the likelihood on the bucket implied by the abstraction's mean equity — chat-Claude C1b decision).

FINDING A.3: BiasConfig.multipliers is exactly shape (len(DiscreteAction),) = (7,) over {FOLD=0, CALL=1, BET_33=2, BET_66=3, BET_100=4, BET_200=5, ALLIN=6} (biased_policy.py:35-44). archetype_policy returns weights = np.zeros(N_DISCRETE_ACTIONS=len(DiscreteAction)=7) (archetypes.py:56, 197, 280). SAME 7 SLOTS — zero translation needed; per-archetype distribution and BiasConfig multiplier vector are index-compatible.

VERIFICATION VERDICT: IMPLEMENTABLE-IN-C1B — archetype-prior path needs no refactor: archetypes.archetype_policy is the existing distribution function; 5 named archetypes, 7-slot output dim-aligned with BiasConfig, deterministic, cheap (microseconds per observation).
