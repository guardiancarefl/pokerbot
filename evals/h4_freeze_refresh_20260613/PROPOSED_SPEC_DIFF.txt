================================================================================
PROPOSED DIFF / PATCH-NOTE for H4_RNR_FIELD_SPEC.md  (FOR MANAGER — DO NOT AUTO-APPLY)
prepared 2026-06-13 by the freeze data-refresh. No commits, no edits made.
Basis: evals/h4_freeze_refresh_20260613/{REFRESH_REPORT.txt, refreshed_constants.json}
================================================================================
Scope note: the task names "§3/§5 targets". The registered moment targets T1-T8
live in the §2.2 table; §4 holds the falsification bars; §3 is the training-design
requirement (unchanged by the data refresh — no edit proposed to §3 itself). The
patch below refreshes §2.2 (replace [DRAFT@272] stamps), corrects §2.5 + §5 OQ-1
status, and adds a freeze block. ALL changes are CI-consistent and Addendum-1
compliant (no threshold weakened; T5/T8 tightened toward a jammier field).

--------------------------------------------------------------------------------
PATCH 1 — §2.2 target table: replace [DRAFT@272] with [FROZEN@517]
--------------------------------------------------------------------------------
Replace the §2.2 marginals table values (old -> new). Drift is within the
dossier CI for every row; these are refinements, not re-statements.

| id | marginal                         | OLD [DRAFT@272]            | NEW [FROZEN@517]                          | grade |
|----|----------------------------------|---------------------------|-------------------------------------------|-------|
| T1 | field VPIP (lower bound)         | 20.6% (±1.9pp)            | 20.1% (689/3426, ±1.3pp) [B1]             | decision |
| T2 | positional shape EP->BTN         | FLAT 22.7-24.5            | FLAT, EP 20.8 / MP 21.4 / CO 26.7 / BTN 22.7 (no widening; CIs overlap) | decision (flatness) |
| T3 | SB elevated / BB defend-floor    | SB 27.8 / BB >= 9.1       | SB 27.9 / BB >= 9.0                        | decision / provisional |
| T4 | open-size mix (non-allin opens)  | 2.0x 55.1; 3.0x ~16; 5x+ ~10; 2.2-2.5 ~5 | <=2.0x 57.8 (±5.7); 3.0x 16.3; 5x+ 10.4; 2.2-2.5 ~3 | decision (2x dominance) |
| T5 | open-jam share of first-raises   | 23.0%                     | 25.5% (99/388, ±4.3pp)                     | decision |
| T6 | preflop entry mix (limp share)   | limps 63% of calls        | limps 63.7% of calls (156/245, ±6.0pp)     | decision (split) |
| T7 | 5-15bb jam rate / seat-hand      | 8.5% (±2.8); 11.8@5-10; 22.2@0-5 | 8.4% (68/812, ±1.9); 11.4@5-10; 19.4@0-5 | decision (band) |
| T8 | jam-regime break                 | 10-15bb; 60-78%<10 -> ~13@10-15 | 10-15bb; 59-83%<10 -> ~20@10-15 (5-10 vs 10-15 CIs disjoint) | decision (boundary) |

Note for the freeze block: the 5-15bb FIELD VPIP-lb companion read moves
20.2% -> 18.8% (153/812, ±2.7pp), inside CI; update the §2.4/§4 narrative if it
cites 20.2.

--------------------------------------------------------------------------------
PATCH 2 — §2.4 acceptance-tolerance check (no re-freeze of registry needed)
--------------------------------------------------------------------------------
Add at the end of §2.4 acceptance: "Freeze refresh (2026-06-13, n=517): every
T1-T8 reproduced inside its dossier Wilson CI; NO constant moved outside CI, so
the moment-match weights and registry_h4_field.json do NOT require re-solving on
numeric grounds (the §5 'large move => re-run moment-match' branch did not
trigger). Re-measure pool aggregate against the refreshed (jammier) T5/T8 only
if the registry is rebuilt for any other reason."

--------------------------------------------------------------------------------
PATCH 3 — §2.5 RNR knob p-cap rationale (MATERIAL: OQ-1 landed)
--------------------------------------------------------------------------------
OLD: "Pre-registered contingency: if OQ-1's fold fix has NOT landed at freeze
time, cap p at 0.25 — the field model's fold dimension is then entirely
profile-inherent rather than measured..."

NEW (proposed): "Freeze status (2026-06-13): OQ-1's fold fix HAS landed; 3
post-fix sessions (220101/230149/023826, 134 opp-obs hands) now capture folds
densely (182/397/263 folded_flag rows vs 1-8 stale pre-fix). Fold-vs-shove is
therefore COMPUTABLE on post-fix data (corpus 82/124 = 66.1%) but NOT yet
decision-grade (Wilson ±8.2pp at the ±5pp field bar; post-fix n still small).
Per the §5 gate ('if decision-grade, add as targets and lift the p=0.25 cap'),
the cap is NOT lifted at this freeze: the fold dimension is measurable-but-sub-
grade, not unmeasured. Keep p capped at 0.25 for the probe (the anchor still
carries the fold dimension), and lift to p=0.30 + add a fold-vs-shove / fold-to-
steal target at the NEXT freeze once post-fix fold data reaches ±5pp (~6 post-
fix sessions at current rates)." [This is a material fold-target trajectory
change -> re-opens §7 R2 review, see PATCH 5.]

--------------------------------------------------------------------------------
PATCH 4 — §5 checklist box marks
--------------------------------------------------------------------------------
- [x] Counter >= 500 confirmed (517 in FIELD_REPORT.txt).
- [x] Recompute every dossier constant on full corpus; §2.2 table regenerated
      with new Wilson CIs/grades (PATCH 1). d2 projected ±1.4pp; observed ±1.3pp.
- [x] CI check: every decision-grade target remains decision-grade; NO constant
      moved outside its dossier CI => no §2.4 re-run / registry re-freeze.
- [~] 3-bet block: opponent 3-bets still rare (anecdote); did NOT reach
      provisional -> leave OUT of moment-match (unchanged). (Re-verify count if
      manager wants; dossier had 26 events, corpus growth modest for 3-bets.)
- [!] OQ-1 status: FIX LANDED + 3 post-fix sessions exist. fold-vs-shove
      computed (66.1%) but NOT decision-grade -> NOT added as target; p stays
      capped per PATCH 3; MATERIAL fold-target trajectory change -> §7 R2 re-open.
- [~] Session-archetype re-check: 18 raw-record sessions now (< 30 threshold);
      tight-3x vs standard-minraise still a sketch -> two-mixture stays FUTURE
      WORK; this run still trains vs the single pooled field. (unchanged)
- [x] Champion identity unchanged (b79e82dd...); e2 hold-row baselines still
      authoritative (no adapter/harness commit touched them — verify before
      probe).
- [ ] Replace [DRAFT@...] stamps + write freeze block (PATCH 6) — manager action.

--------------------------------------------------------------------------------
PATCH 5 — §7 R2 disposition addendum (post-fix evidence now exists)
--------------------------------------------------------------------------------
Append to R2: "Update (freeze, 2026-06-13): OQ-1 landed; 3 post-fix sessions
make fold-vs-shove computable (66.1%, ±8.2pp). It is NOT yet a target (sub-
grade), so R2's risk is unchanged in KIND but now has an empirical anchor: the
field's observed facing-allin fold rate (~66%) is high, consistent with steal-
EV being a large exploitation term — reinforcing the §4.4 aggression-inflation
caps and the p<=0.25 anchor load. The mitigation set (i)-(iv) stands; (iv)
['upgrade the target the moment post-fix fold data is decision-grade'] now has a
concrete trigger (~6 post-fix sessions)."

--------------------------------------------------------------------------------
PATCH 6 — new FREEZE BLOCK (manager writes; values supplied)
--------------------------------------------------------------------------------
Add after §0/header:

  FROZEN 2026-06-13 at counter 517 (>= 500). Substrate: 23 sessions / 18
  raw-record / 3426 non-hero seat-hands / 1041 opp voluntary frame_diff actions.
  §2.2 targets refreshed per PATCH 1 (all inside dossier CI; none re-stated).
  §4 bars unchanged (relative to champion baselines re-recorded at freeze).
  Registry registry_h4_field.json NOT re-frozen (no constant moved outside CI).
  OQ-1: fix landed, fold-vs-shove sub-grade -> p capped at 0.25 (PATCH 3).
  Refresh evidence: evals/h4_freeze_refresh_20260613/.
================================================================================
